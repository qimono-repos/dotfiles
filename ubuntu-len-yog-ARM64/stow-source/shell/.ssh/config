# GitHub push key for this Snapdragon Yoga (authenticated as qimono-repos).
# IdentitiesOnly keeps the agent from offering unrelated keys first.
Host github.com
    IdentityFile ~/.ssh/id_ed25519_git_repos
    IdentitiesOnly yes
