# Startup — cd to dotfiles repo on every new shell
# (firefox is launched once per login via ~/.config/autostart/60-startup.desktop)
# Stow: shell → ~/.zshrc.d/60-startup.zsh

cd "$HOME/source/repos/qimono-repos/dotfiles/" # && vi .
