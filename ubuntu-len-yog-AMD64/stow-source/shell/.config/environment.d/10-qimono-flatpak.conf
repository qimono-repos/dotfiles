# GNOME / systemd user session (does not read ~/.zshrc).
# Foreign-distro: Guix flatpak client + Ubuntu-blessed bwrap.
# See ubuntu-mini-pc/docs/flatpak-guix.md
FLATPAK_BWRAP=/usr/bin/bwrap

# Flatpak export dirs must be visible to the SESSION bus/manager, not just
# interactive shells (10-guix.zsh only fixes shells). Without these, every
# tile-launched login shell prints the "directories not in XDG_DATA_DIRS"
# note, and D-Bus service files under exports/ stay invisible.
XDG_DATA_DIRS="${XDG_DATA_DIRS}:/var/lib/flatpak/exports/share:${HOME}/.local/share/flatpak/exports/share"
