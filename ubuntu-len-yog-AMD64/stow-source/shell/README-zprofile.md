# Login PATH for Guix

Do **not** stow a full `~/.zprofile` over an existing machine file.

Merge the contents of `zprofile-guix-snippet.zsh` into `~/.zprofile` once
(or let `setup` docs do it). Already applied on the Yoga 2026-08 session.
