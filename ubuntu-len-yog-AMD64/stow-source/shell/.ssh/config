# GitHub push key (no default-name match otherwise). IdentitiesOnly keeps
# the agent from offering unrelated keys first.
Host github.com
    IdentityFile ~/.ssh/id_ed_key_25519_qimono_ubuntu__for__github_qimono_repos
    IdentitiesOnly yes
