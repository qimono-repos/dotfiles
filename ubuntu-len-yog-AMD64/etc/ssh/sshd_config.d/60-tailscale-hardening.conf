# Tailscale-era hardening for qimono-localhost
# Proposal: ~/TAILSCALE-README.md Part 4 · AGENTS.md todo "SSH hardening"
# Installed by scripts/install-ssh-hardening.sh — never hand-edit here AND there.
# Keys only (TAILSCALE-README safety rule 1); brute-force bots get nothing.
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin no
